[build]
  # Frontend build
  base = "frontend"
  publish = "dist/frontend/browser"
  command = "npm run build"

# SPA fallback - all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

